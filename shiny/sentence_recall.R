sentence_recall <-
  tabItem(tabName = "sentence_recall",
          fluidRow(
            box(
              title = tags$b("Instructions"),
              collapsible = TRUE,
              solidHeader = TRUE,
              status = "primary",
              width = 12,
              p("STUFF AND THINGS")
            ) # box
          ) #fluidrow
  ) #close page
