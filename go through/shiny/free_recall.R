free_recall <-
  tabItem(tabName = "free_recall",
          fluidRow(
            box(
              title = tags$b("Instructions"),
              collapsible = TRUE,
              solidHeader = TRUE,
              status = "success",
              width = 12,
              p("STUFF AND THINGS")
            ) # box
          ) #fluidrow
          ) #close page
